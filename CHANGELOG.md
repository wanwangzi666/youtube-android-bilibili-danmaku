# 更新日志

本项目遵循语义化版本：`1.x` 期间只做修复与小幅功能增减，接口与设置项保持向后兼容。

下载：<https://github.com/wanwangzi666/youtube-android-bilibili-danmaku/releases>

---

## 1.1.4

**新增：播放页面板里的全局总开关**

用户要求「在悬浮窗里增加一个全局设置，也就是全局开关是否匹配弹幕，而不是只限定于短视频区」。
顺带发现 `enabled` 这个设置项**从来没有被真正消费** —— `DanmakuView.setSettings()` 没读它，
自动匹配也不看它，所以设置页的「启用弹幕」和面板里的「关闭弹幕 / 开启弹幕」其实都是空操作。

- 播放页面板顶部新增 **「全局设置（所有视频生效，立即保存）」** 分组，含两个开关：
  - **匹配并显示弹幕（总开关）** —— 关掉后不再自动匹配、弹幕层隐藏
  - **在 Shorts（竖屏短视频）里也匹配** —— 只影响竖屏短视频流
- 两个开关都走**运行时通道**（被注入进程自己的 SharedPreferences），点一下立即生效并保存，
  不依赖此前实测不可靠的跨进程设置读取
- 总开关关掉时**保留悬浮「弹」按钮** —— 否则用户就没法把它开回来了
- 总开关关掉时自动匹配全线停止（4 道闸门都加了这条），并关闭可能开着的选择弹窗、
  清空已加载弹幕；下载中途关掉总开关也会丢弃结果
- 面板「显示」分组里去掉了与原「关闭弹幕」重复的按钮，只留「清空弹幕」+「重新搜索」
- 诊断区新增 `总开关 / Shorts 匹配` 两行状态，并把 Shorts 那行改写成
  `overlay.shortsDiagnosticsLine()`，一眼看清「判定结果」与「开关状态」
- 模块设置页的「启用弹幕（总开关）」也读写运行时副本，标注了与面板开关的关系
- 新增 3 个单测覆盖总开关的默认值与优先级规则（单测总数 134 → 137）

## 1.1.3

**修复「设置里取消了勾选，YouTube 里却仍然匹配 Shorts」**

诊断信息显示 `已识别为 Shorts，但设置=允许匹配` —— **识别是好的，坏的是设置读取**：
模块 App 保存的 `matchInShorts` 没能通过 `XSharedPreferences` 同步到被注入的 YouTube 进程
（这条跨进程路径在部分 Vector / LSPosed / Android 版本组合下读不到最新值，
表现就是勾选框怎么点都没反应）。

修法是给这个开关加一条**不依赖跨进程机制**的通道：

- 新增**运行时开关**：被注入进程把自己那份值写进自己的 SharedPreferences 文件
  （`b2y_runtime`），自己写自己读，必定同步。生效优先级为
  **运行时开关 > 模块 App 的 JSON 配置**
- **播放页面板顶部新增全局开关**「在 Shorts 里也匹配弹幕」：立即生效、立即保存，
  不需要重启 YouTube，也不依赖设置页那条路径
- 模块设置页保存时也会写运行时那份，两个入口显示的状态始终一致
- 诊断行改写清楚：区分「未识别为 Shorts」「已识别 + 开关关闭 → 已屏蔽」
  「已识别但开关开启」，一眼看出是判定问题还是开关问题
- 新增 2 个单测覆盖生效值优先级规则（运行时开关压过配置文件）

## 1.1.2

**改用「底部导航的 Shorts 标签是否选中」判断是否在 Shorts 里**（用户建议的判定方式）

1.1.1 里勾选与取消勾选都"没有效果"，说明不是设置问题，而是**判定没命中**。
本版本增加一层更可靠的判定，并调整判定顺序：

- 三层判定：**Shorts 标签选中 + 竖屏画面** / Shorts 播放器视图 / 竖屏铺满几何
- 标签判定只用框架 API（`findViewsWithText` + `selected` 状态）遍历视图树，
  **不 hook YouTube 任何内部类**，且要求标签位于屏幕下半部分，
  避免把标题里的 `#shorts` 当成导航标签
- 自动匹配的闸门加到 4 处：新增「弹幕下载完成、准备贴到画面前」的检查 ——
  下载期间滑进 Shorts 就直接丢弃结果
- 设置页把该项拆成独立分组「Shorts（竖屏短视频）」，文案改为
  「在 Shorts 里也匹配并显示弹幕」（原文案又长又绕）
- 新增 8 个单测覆盖标签判定（大小写/空格、未选中、标题里的 shorts、其它标签选中、
  底部区域约束等）

## 1.1.1

**默认值改成「Shorts 里不匹配弹幕」，并修掉屏蔽不生效的问题**

1.1.0 把这个开关默认设成了**开**（为了保持旧行为），结果刷 Shorts 依然会弹
「选择要同步的 B 站视频」。两处一起改：

**默认值改为「不匹配」**

- 全新安装：默认不匹配
- 配置里没有这个键的旧版本升级时按新默认值读取（自动迁移）
- 显式设置过的用户以用户选择为准
- 设置页标注「默认关闭」并补说明

**屏蔽闸门从 1 处加到 3 处**

1.1.0 只在「发起搜索」那一刻判断，而搜索是异步的 —— 结果回来时用户可能已经滑进 Shorts，
弹窗照样冒出来。现在：

- 发起自动匹配时（原有）
- 准备显示选择弹窗时（新增，最贴近用户看到界面的那层）
- 浮层每轮刷新时（新增）：已经滑进 Shorts 但弹窗还开着，自动关掉并清空弹幕

**判定不再依赖时序**

- 原先只看浮层每 250ms 喂进来的画面矩形，在此之前判定恒为「不是 Shorts」；
  现在浮层没有数据时直接向 `VideoSurfaceTracker` 取
- 「当前在哪个界面」改用最近 `onActivityResumed` 的 Activity，不再依赖浮层是否已挂载

## 1.1.0

**新增：Shorts（竖屏短视频）识别与跳过**

- 新增 `ShortsDetector`，用两层信号判断是否在 Shorts：Shorts 播放器类名
  （`reel.internal.*`，R8 保留原名）+ 竖屏铺满几何
- 模块设置与播放页悬浮面板里都能切换「Shorts 也匹配弹幕」
- 关掉后：进入 Shorts 不发起搜索，已加载弹幕与悬浮按钮一起隐藏；
  面板里的手动加载不受影响

## 1.0.0

首个 Release。

- 自动识别 YouTube 视频（DEX 指纹取视频 ID + oEmbed 取原始标题）
- 按标题三重打分匹配 B 站视频，超阈值自动加载
- 分段拉取并解析 protobuf 弹幕，自绘 Canvas 渲染
- 播放位置取自内部时间指纹，回退 `MediaSession` 外推；暂停/倍速/拖动天然同步
- 支持番剧（PGC）、时间轴偏移、手动粘贴 B 站链接
- 播放页控制面板与诊断信息
- 固定发布密钥签名，后续版本可直接覆盖升级
